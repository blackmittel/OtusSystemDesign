=== Потоки данных и высокоуровневое взаимодействие

==== Информационные потоки

=== Потоки данных и высокоуровневое взаимодействие
[NOTE]
====
Раздел описывает движение данных между компонентами системы.
Указаны источник, приёмник, тип данных и способ передачи.
====
==== Потоки данных
[cols="1,3,2,3",options="header"]
|===
| ID | Источник -> Приёмник | Тип данных | Способ
| IF-01 | Client -> GeoDNS -> API Gateway | HTTP request | Sync
| IF-02 | Client -> CDN | Media | Sync
| IF-03 | API Gateway -> Auth Service | JWT token, session_id | Sync
| IF-04 | Auth Service -> PostgreSQL | Credentials (login, password hash) | Sync
| IF-05 | Auth Service -> Redis | Session, token | Sync
| IF-06 | API Gateway -> Redis | Session lookup | Sync
| IF-07 | API Gateway -> User Service | Profile data | Sync
| IF-08 | User Service -> PostgreSQL | User data | Sync
| IF-09 | API Gateway -> Ticket Service | Ticket data | Sync
| IF-10 | Ticket Service -> PostgreSQL | Ticket data | Sync
| IF-11 | Ticket Service -> S3 | Media binary | Sync
| IF-12 | S3 -> CDN | Media object | Async
| IF-13 | Ticket Service -> Kafka | ticket.events | Async
| IF-14 | Kafka -> Search Service | ticket.events | Async
| IF-15 | Search Service -> ElasticSearch | Index data | Sync
| IF-16 | API Gateway -> Search Service | Search query | Sync
| IF-17 | API Gateway -> Order Service | Order data | Sync
| IF-18 | Order Service -> PostgreSQL | Order data | Sync
| IF-19 | Order Service -> Ticket Service | Reserve request | Sync
| IF-20 | Order Service -> Payment Service | Payment request | Sync
| IF-21 | Order Service -> Delivery Service | Delivery request | Sync
| IF-22 | Payment Service -> PostgreSQL | Payment data | Sync
| IF-23 | Payment Service -> External Payment API | Payment request | Sync
| IF-24 | External Payment API -> Payment Service | Payment status webhook | Async
| IF-25 | Payment Service -> Kafka | payment.events | Async
| IF-26 | Kafka -> Order Service | payment.events | Async
| IF-27 | Delivery Service -> PostgreSQL | Delivery data | Sync
| IF-28 | Delivery Service -> External Logistics API | Shipment request | Sync
| IF-29 | External Logistics API -> Delivery Service | Delivery status webhook | Async
| IF-30 | Delivery Service -> Kafka | delivery.events | Async
| IF-31 | Kafka -> Order Service | delivery.events | Async
| IF-32 | Order Service -> Kafka | order.events | Async
| IF-33 | Kafka -> Billing Service | order.events | Async
| IF-34 | Billing Service -> PostgreSQL | Billing data | Sync
| IF-35 | Billing Service -> Payment Service | Payout request | Sync
| IF-36 | Ticket Service -> Kafka | message.events | Async
| IF-37 | Kafka -> Analytics Service (ClickHouse) | ticket.events, order.events, payment.events, delivery.events, message.events | Async
|===
==== Схема взаимодействия

ifndef::imagesdir[:imagesdir: {docdir}/images]

image::Interaction scheme.png[]
