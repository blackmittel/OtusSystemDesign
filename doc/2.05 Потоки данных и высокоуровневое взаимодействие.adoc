=== Потоки данных и высокоуровневое взаимодействие

[cols="15,20,30,15,15,20", options="header"]
|===
| iD | Поток данных | Участники | Протокол | Тип | Описание

| IF-1
| Создание/редактирование объявления
| API Gateway → сервис авторизации
| HTTP/REST
| Синхронный
| Валидация пользователя и прав
| IF-2
|| API Gateway → сервис объявлений
| HTTP/REST
| Синхронный
| Сохранение объявления в БД
| IF-3
|| сервис объявлений → сервис поиска
| Async Events
| Асинхронный
| Обновление поискового индекса
| IF-4
|| сервис объявлений → кэш
|
| Синхронный
| Обновление кэша

| IF-5
| Поиск объявлений
| API Gateway → сервис поиска
| HTTP/REST
| Синхронный
| Обработка поискового запроса
| IF-6
|| сервис поиска → БД поиска
| HTTP
| Синхронный
| Выполнение поиска в индексе
| IF-7
|| сервис поиска → кэш
|
| Синхронный
| Кэширование результатов поиска

| IF-8
| Создание заказа
| API Gateway → сервис авторизации
| HTTP/REST
| Синхронный
| Валидация пользователя
| IF-9
|| API Gateway → сервис заказов
| HTTP/REST
| Синхронный
| Инициирование создания заказа
| IF-10
|| сервис заказов → сервис объявлений
| HTTP/REST
| Синхронный
| Проверка доступности и резервирование
| IF-11
|| сервис заказов → сервис платежей
| HTTP/REST
| Синхронный
| Инициирование платежа
| IF-12
|| сервис заказов → сервис доставки
| HTTP/REST
| Синхронный
| Создание доставки
| IF-13
|| сервис заказов → сервис объявлений
| Async Events
| Асинхронный
| Уведомление о создании заказа

| IF-14
| Обработка платежа
| сервис платежей → Payment Gateway
| HTTP
| Синхронный
| Вызов внешнего платежного шлюза
| IF-15
|| Payment Gateway → сервис платежей
| HTTP Webhooks
| Асинхронный
| Обратный вызов о статусе платежа
| IF-16
|| сервис платежей → сервис заказов
| Async Events
| Асинхронный
| Обновление статуса заказа
| IF-17
|| сервис платежей → аналитический сервис
| Async Events
| Асинхронный
| Фиксация платежного события

| IF-18
| Управление доставкой
| сервис доставки → API служб доставки
| HTTP/REST
| Синхронный
| Создание заявки в логистической компании
| IF-19
|| API служб доставки → сервис доставки
| HTTP Callbacks
| Асинхронный
| Обновление статуса доставки
| IF-20
|| сервис доставки → сервис заказов
| Async Events
| Асинхронный
| Обновление статуса заказа
| IF-21
|| сервис доставки → сервис объявлений
| Async Events
| Асинхронный
| Уведомление об изменении статуса доставки

| IF-22
| Обновление поискового индекса
| шина → сервис поиска
| AMQP
| Асинхронный
| Получение события об изменении объявления
| IF-23
|| сервис поиска → БД поиска
| HTTP
| Синхронный
| Обновление поискового индекса

| IF-24
| Сбор аналитики
| Все сервисы → шина
| Async Events
| Асинхронный
| Отправка бизнес-событий в очередь
| IF-25
|| шина → аналитический сервис
| AMQP
| Асинхронный
| Чтение событий для анализа
| IF-26
|| аналитический сервис → ClickHouse
| HTTP/Batch
| Пакетный
| Загрузка агрегированных данных в OLAP

| IF-27
| Нотификации пользователей
| шина → сервис объявлений
| AMQP
| Асинхронный
| Получение событий для нотификаций и обновления в ЛК пользователя

| IF-28
| Резервирование товаров
| сервис заказов → сервис объявлений
| HTTP/REST
| Синхронный
| Запрос на резервирование товара

| IF-29
| Запрос отчетов
| API Gateway → аналитический сервис
| SQL
| Синхронный
| Выполнение аналитических запросов
|===
